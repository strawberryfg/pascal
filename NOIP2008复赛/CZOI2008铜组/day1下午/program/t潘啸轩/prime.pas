var a:array[1..n,1..n] of integer;
    n,i,j:integer;
begin
  assign(input,'prime.in');
  reset(input);
  assign(output,'prime.out');
  rewrite(output);
  readln(n);
  a[1,j]:=1;
  if a[i]+a[i+1]=
  
   