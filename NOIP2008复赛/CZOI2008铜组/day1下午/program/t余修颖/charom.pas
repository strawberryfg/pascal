var n,i,m,n:longint;
    a:array[1..26]of char;
    ch:char
begin
        assign(input,'charom.in');
        reset(input);
        assign(output,'charom.out');
        rewrite(output);

        readln(n);
        i:=0;
        while not eof do
        begin
        inc(i);
        readln(a[i]);
        writeln(m)

        close(input);
        close(output);
end.
