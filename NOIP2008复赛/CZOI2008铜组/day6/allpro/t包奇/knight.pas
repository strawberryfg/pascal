program knight;
var m,n,x,y:integer;
begin
  assign(input,'knight.in');
  reset(input);
  assign(output,'knight.out');
  rewrite(output);
  readln(m,n,x,y);
  writeln(No Answer!);
  close(input);
  close(output);
end.