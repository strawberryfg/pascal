var n,m,x,y:longint;
begin
  assign(input,'knight.in');
  reset(input);
  assign(output,'knight.out');
  rewrite(output);
  read(n,m,x,y);
  writeln('No Answer!');
  close(input);
  close(output);
end.