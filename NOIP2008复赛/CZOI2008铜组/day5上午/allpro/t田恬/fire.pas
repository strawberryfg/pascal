var a:array[1..10000]of int64;
    c:integer;

function place(i,j:longint): boolean;
  var n,k:longint;
    begin
      for i:=1 to n do
      if a[k]=j then place:=false;
          if a[k-1]=i-j then place:=false;
             if c mod 10<>0  then place:=true;
             readln(a[n]);
    end;
procedure writeout;
   var n,i,j:integer;
    begin
for i:=1 to n do
    end;
procedure try(i:integer);
  var n,j:integer;
    begin
    assign(input,'fire.in');
reset(input);
assign(output,'fire.out');
rewrite(output);
      if place(i,j) then
                           writeout
                    else begin
                           readln(n);
                           c:=0;
                           try(1);
                           writeln(c);
close(input);
close(output);
end.