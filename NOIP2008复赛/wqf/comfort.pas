begin
{assign(input,'comfort.in');
reset(input);
assign(output,'comfort.out');
rewrite(output);}
readln(n,z,m);
for i:=1 to m do
    begin
    read(t);
    inc(tot);
    a[tot]:=t;
    end;
k:=1;
pd:=false;
while pd=false do
      begin

      end;
close(input);
close(output);
end.