{var n,i,j:longint;
    a:array[1..10,1..2] of ansistring;
    b:array[1..]}
begin
assign(input,'friends.in');
reset(input);
assign(output,'friends.out');
rewrite(output);
{readln(n);
for i:=1 to n do
    begin
    read(a[i,1])
    read(' ');
    read(a[i,2])
    end;
for i:=1 to}
writeln('friends');
writeln('almost friends');
writeln('almost friends');
writeln('nothing');
close(input);
close(output); 
end.